# Podcast Platform - Environment Variables
# Copy to .env and adjust as needed

PORT=3000
HOST=0.0.0.0

# Public URL for API, RSS, and static assets
# For public domain: https://podcast.bbs2-wob.de
# For local: http://localhost:3000
PUBLIC_URL=http://localhost:3000

# JWT secret for authentication (change in production!)
JWT_SECRET=change-me-in-production

# Database: use either SQLite (local/demo) or PostgreSQL (production)
# SQLite - local file (default when DATABASE_URL not set)
DB_PATH=./data/podcasts.db

# PostgreSQL - for production or remote DB (when set, overrides DB_PATH)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS allowed origins (optional; defaults to PUBLIC_URL)
# Comma-separated for multiple: https://a.de,https://b.de
# CORS_ORIGIN=
